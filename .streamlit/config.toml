# Finance OS never phones home. This disables Streamlit's own usage-stats
# telemetry ping (the only outbound network behavior Streamlit has by default).
[browser]
gatherUsageStats = false

[server]
headless = true

[theme]
primaryColor = "#2E7D32"
