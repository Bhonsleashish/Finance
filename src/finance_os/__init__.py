"""Finance OS: a local-first, offline personal finance operating system.

Nothing in this package makes network calls. All data lives in a single
SQLite database on disk (see `finance_os.config.Settings.database_file`).
"""

__version__ = "0.1.0"
